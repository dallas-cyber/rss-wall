(function () {
  function loadWall() {
    var wall = document.createElement("rssapp-wall");
    wall.setAttribute("id", "_Csr2yTzt0cnf3Q7C");
    document.body.appendChild(wall);
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", loadWall);
  } else {
    loadWall();
  }
})();
